steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/lobehub-custom/lobehub:latest'
      - '-t'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/lobehub-custom/lobehub:${SHORT_SHA}'
      - '.'
    timeout: '3600s'  # 1 hour timeout for large build

images:
  - 'us-central1-docker.pkg.dev/$PROJECT_ID/lobehub-custom/lobehub:latest'
  - 'us-central1-docker.pkg.dev/$PROJECT_ID/lobehub-custom/lobehub:${SHORT_SHA}'

options:
  machineType: 'E2_HIGHCPU_8'
  logging: CLOUD_LOGGING_ONLY

timeout: '3600s'
